{
  "name": "social-media-mother",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A welcoming, creator-first social platform by Ali Suleman.",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently -n api,web -c magenta,cyan \"npm run dev --workspace server\" \"npm run dev --workspace client\"",
    "build": "npm run build --workspace client",
    "start": "npm run start --workspace server",
    "check": "npm run check --workspace server && npm run test --workspace client && npm run build --workspace client"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
